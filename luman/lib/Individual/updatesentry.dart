import 'package:flutter/material.dart';
import 'package:luman/Individual/updatespage.dart';

class UpdatesPage extends StatelessWidget {
  //final username;
  const UpdatesPage({
    Key? key,
    //required this.username,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
