import 'package:flutter/material.dart';
import 'package:luman/Individual/chatbot.dart';

class ChatbotPage extends StatelessWidget {
  const ChatbotPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
